apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard
  namespace: aegis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dashboard
  template:
    metadata:
      labels:
        app: dashboard
    spec:
      containers:
        - name: dashboard
          image: aegis-dashboard:latest # Replace with your registry image
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: aegis
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: dashboard
